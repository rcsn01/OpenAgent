# Voice Mode

Voice UI support is retained through the Electron native shell.

The desktop preload exposes speech model listing, model preparation, capture sessions, audio sample streaming, level events, chunk and turn transcription, and one-shot transcription. These APIs are still frontend-facing only; they do not require the removed backend runtime.
